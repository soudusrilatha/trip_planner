<% if (locals.messages) { %>
  <% if (messages.success && messages.success.length > 0) { %>
    <div id="flash-success" class="fixed top-20 right-4 z-50 glass px-5 py-3 rounded-xl border border-green-500/30 text-green-400 text-sm font-medium flex items-center gap-2 shadow-lg animate-bounce" style="animation-iteration-count:2">
      <i class="fas fa-check-circle"></i> <%= messages.success[0] %>
      <button onclick="this.parentElement.remove()" class="ml-2 text-green-600 hover:text-green-400"><i class="fas fa-times"></i></button>
    </div>
    <script>setTimeout(()=>{const el=document.getElementById('flash-success');if(el)el.remove();},4000);</script>
  <% } %>
  <% if (messages.error && messages.error.length > 0) { %>
    <div id="flash-error" class="fixed top-20 right-4 z-50 glass px-5 py-3 rounded-xl border border-red-500/30 text-red-400 text-sm font-medium flex items-center gap-2 shadow-lg">
      <i class="fas fa-exclamation-circle"></i> <%= messages.error[0] %>
      <button onclick="this.parentElement.remove()" class="ml-2 text-red-600 hover:text-red-400"><i class="fas fa-times"></i></button>
    </div>
    <script>setTimeout(()=>{const el=document.getElementById('flash-error');if(el)el.remove();},4000);</script>
  <% } %>
<% } %>
